import styled from "styled-components";

// Create a styled component
const Card = styled.img`
	position: absolute;
	top: 1rem;
	border-radius: 100%;
	height: 4rem;
	width: 4rem;
	margin: 1rem;
`;

export default Card;
