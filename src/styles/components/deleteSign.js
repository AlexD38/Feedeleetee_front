import styled from "styled-components";

// Create a styled component
const DeleteSign = styled.svg`
	margin-left: 2rem;
	visibility: hidden;
	&:hover {
		color: red;
	}
`;

export default DeleteSign;
