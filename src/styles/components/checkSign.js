import styled from "styled-components";

// Create a styled component
const checkSign = styled.svg`
	// margin: 0 1rem;
	padding: 0;
	position: relative;
	right: 0;
	top: 0;
	transition: all 0.2s ease;

	&:hover {
		cursor: pointer;
		color: #eca869;
		transition: all 0.2s ease;
	}
`;

export default checkSign;
